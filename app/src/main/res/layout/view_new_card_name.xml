<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
       xmlns:app="http://schemas.android.com/apk/res-auto"
       xmlns:tools="http://schemas.android.com/tools"
       tools:showIn="@layout/view_new_card"
    >

    <TextView
        style="@style/CustomText.Black.Title"
        android:text="@string/newcard_name_title"
        />

    <View
        android:layout_width="wrap_content"
        android:layout_height="2dp"
        android:background="@drawable/shadow_reverse"
        />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:gravity="center"
        android:orientation="horizontal"
        >

        <EditText
            android:id="@+id/name_field"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@color/transparent"
            android:hint="@string/newcard_name_hint"
            android:textColorHint="@android:color/black"
            android:paddingLeft="16dp"
            android:textSize="16sp"
            />

        <ImageView
            android:id="@+id/clear_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:srcCompat="@drawable/ic_action_close"
            android:layout_marginRight="16dp"
            tools:ignore="ContentDescription"
            />
    </LinearLayout>

    <View
        android:layout_width="wrap_content"
        android:layout_height="2dp"
        android:background="@drawable/shadow"
        />
</merge>